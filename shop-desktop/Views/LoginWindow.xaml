<Window x:Class="shop_desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Login:" Margin="0,0,5,0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtUsername" Margin="0,0,0,5" Width="150"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Password:" Margin="0,5,5,0" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtPassword" Margin="0,5,0,5" Width="150"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Login" Click="Login_Click" Margin="0,0,5,0"/>
            <Button Content="Register" Click="Register_Click"/>
        </StackPanel>
    </Grid>
</Window>
